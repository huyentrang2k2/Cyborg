<%- include('component/header') -%>
<div class="container-fluid pt-4 px-4">
	<div class="row g-4">
		<div class="col-12">
			<form class="bg-light rounded h-100 p-4" method="post" enctype="multipart/form-data">
				<h6 class="mb-4">Add-product</h6>
				<div class="form-floating mb-3">
					<input type="text" class="form-control" name="name" id="name" />
					<label for="floatingInput">Name</label>
				</div>
				<div class="form-floating mb-3">
					<input type="number" class="form-control" name="price" id="price" />
					<label for="floatingPassword">Price</label>
				</div>
				<div class="form-floating mb-3">
					<input type="number" class="form-control" name="quantity" id="quantity" />
					<label for="floatingPassword">Quantity</label>
				</div>
				<div class="form-floating mb-3">
					<textarea
						class="form-control"
						placeholder="Leave a comment here"
						name="describe"
						id="describe"
						style="height: 150px"
					></textarea>
					<label for="floatingTextarea">Describe</label>
				</div>
				<div class="form-floating mb-3">
					<input type="file" class="form-control" name="productImage" id="productImage" />
					<label for="floatingPassword">Images</label>
				</div>
				<!-- <div class="form-floating mb-3">
					<select
						class="form-select"
						name="categorys"
						id="Select"
						aria-label="Floating label select example"
					></select>
					<label for="floatingSelect">Categorys</label>
				</div>
				<div class="form-floating mb-3">
					<select
						class="form-select"
						name="classifys"
						id="floatingSelect"
						aria-label="Floating label select example"
					></select>
					<label for="floatingSelect">Classifys</label>
				</div> -->
				<button type="submit" class="btn btn-primary btn-block mb-4">Add</button>
			</form>
		</div>
	</div>
</div>
<script>
	// const getData = () => {
	// 	axios
	// 		.get('http://localhost:3000/api/list_cate')
	// 		.then((response) => {
	// 			console.log(response.data);
	// 			const row = document.querySelector('#Select');
	// 			response.data.forEach((item, index) => {
	// 				const div = document.createElement('option');
	// 				div.value = item.id;
	// 				div.innerHTML = `
	// 				<p>${item.name}</p>
	// 							`;
	// 				div.setAttribute('name', 'category');
	// 				row.appendChild(div);
	// 			});
	// 		})
	// 		.catch((err) => console.log(err.response.data.message));
	// };

	// getData();

	// const Data = () => {
	// 	axios
	// 		.get('http://localhost:3000/api/list_classifys')
	// 		.then((response) => {
	// 			console.log(response.data);
	// 			const row = document.querySelector('#floatingSelect');
	// 			response.data.forEach((item, index) => {
	// 				const div = document.createElement('option');
	// 				div.value = item.id;
	// 				div.innerHTML = `
	// 				<p>${item.name}</p>
	// 							`;
	// 				div.setAttribute('name', 'classify');
	// 				row.appendChild(div);
	// 			});
	// 		})
	// 		.catch((err) => console.log(err.response.data.message));
	// };

	// Data();
</script>
<%- include('component/footer') -%>
